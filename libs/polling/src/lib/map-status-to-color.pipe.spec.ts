import { MapStatusToColorPipe } from './map-status-to-color.pipe';

describe('MapStatusToColorPipe', () => {
  it('create an instance', () => {
    const pipe = new MapStatusToColorPipe();
    expect(pipe).toBeTruthy();
  });
});
